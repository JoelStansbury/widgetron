{
    "menu_name": "{{name}}",
    "menu_items": [
        {
            "name": "{{name}}",
            "system": "cmd.exe /c powershell -WindowStyle {{ window_style }} -Command",
            "scriptarguments": [
                "cmd.exe /c '${PREFIX}/Scripts/activate.bat && {{ name_nospace }}'"
            ],
            "icon": "${MENU_DIR}/{{icon_name}}"
        }
    ]
}