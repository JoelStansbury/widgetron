@ECHO on
%PYTHON% -m pip install . -vv --no-deps --no-build-isolation
set MENU_DIR="%PREFIX%\Menu"
if not exist %MENU_DIR% mkdir %MENU_DIR%
copy "%RECIPE_DIR%\widgetron_shortcut.json" %MENU_DIR%
copy "%RECIPE_DIR%\{{icon_name}}" %MENU_DIR%