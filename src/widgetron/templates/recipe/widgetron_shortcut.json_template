{
    "menu_name": "{{name}}",
    "menu_items": [
        {
            "name": "{{name}}",
            "system": "cmd.exe /c powershell -WindowStyle Hidden -Command",
            "scriptarguments": [
                "cmd.exe /c '${PREFIX}/Scripts/activate.bat && pythonw -m widgetron_app'"
            ],
            "icon": "${MENU_DIR}/{{icon_name}}"
        }
    ]
}