{
    "menu_name": "{{name}}",
    "menu_items": [
        {
            "name": "{{name}}",
            "system": "cmd.exe",
            "scriptarguments": ["/c start /min cmd.exe /c \"${PREFIX}/Scripts/activate.bat && {{name_nospace}}\""],
            "icon": "${MENU_DIR}/{{icon_name}}"
        }
    ]
}