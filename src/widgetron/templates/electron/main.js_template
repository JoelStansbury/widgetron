const { app, BrowserWindow } = require('electron')

url = process.env.widgetron_url;

const createWindow = () => {
  const win = new BrowserWindow({
    width: 1000,
    height: 600
  })
  win.loadURL(url)
  }
app.whenReady().then(() => {
  createWindow()
})