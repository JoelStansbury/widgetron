package:
  name: widgetron_app
  version: 0.0.1

source:
  url: '{{ environ.SDIST_URL }}'
  sha256: '{{ environ.SDIST_SHA256 }}'

build:
  string: 'py0_{{ environ.SDIST_SHA256[:7] }}'
  ignore_prefix_files:
    - Lib/site-packages/widgetron_app/*.zip


requirements:
  host:
    - pip
    - python
  run:
    - python