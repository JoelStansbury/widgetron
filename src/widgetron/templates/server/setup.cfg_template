[metadata]
name = widgetron_app
description = Holds the build electron package

[options]
zip_safe = False
packages = find:

[options.package_data]
widgetron_app = **

[options.entry_points]
    console_scripts = 
        {{name}} = widgetron.cli:main