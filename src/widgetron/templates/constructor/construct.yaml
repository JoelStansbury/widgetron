name: {{name}}
version: {{version}}
channels:{% for c in channels %}
  - {{c}}{% endfor %}
  - local  # allows to find the generated conda package
  - conda-forge
specs:
  - widgetron_app
register_python_default: false