{
    "menu_name": "{{name}}",
    "menu_items": [
        {
            "name": "{{name}}",
            "system": "cmd.exe /c {% if hide_cmd_prompt %}powershell -WindowStyle Hidden -Command{% endif %}",
            "scriptarguments": [
                "cmd.exe /c '${PREFIX}/Scripts/activate.bat && {{ name_nospace }}'"
            ],
            "icon": "${MENU_DIR}/{{icon_name}}"
        }
    ]
}