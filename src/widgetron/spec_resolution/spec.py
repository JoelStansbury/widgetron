from hashlib import sha256
import json
import os
import platform
import re
import shutil
import sys
from pathlib import Path
from subprocess import Popen, PIPE, call



def handle_lock(kwargs):

def _method(kwargs):
    if kwargs.get("environment_lock"): return handle_lock
    if kwargs.get("environment_yaml"): return handle_lock
