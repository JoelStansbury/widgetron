{
    "menu_name": "{{name}}",
    "menu_items": [
        {
            "name": "{{name}}",
            "script": "${PREFIX}/Scripts/activate.bat && python -m widgetron_app",
            "icon": "${MENU_DIR}/widgetron_icon.ico"
        }
    ]
}